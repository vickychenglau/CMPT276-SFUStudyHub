Enter course name in the format "CMPT276: Introduction to Software Engineering"
<%= form_for(@course) do |c| %>
	<%= c.label :name %>
	<%= c.text_field :name %><br><br><br>
	<%= c.submit %><br><br>
<% end %>